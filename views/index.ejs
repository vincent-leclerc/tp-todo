<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />

  </head>
  <body>
    <h1><%= title %></h1>

    <div class="item">
      <ul>
        <% for (item of items) { %>
          <% if (!item.isDone) { %>
            <li>
              <a href ="/remove/<%= item.id %>">✘</a>
              
              <!--
                Il ne se modifie pas si c'est la balise p que 
                je modifie dans style.css ou dans un id ou une classe ?? 
              -->
              <a href="/update/<%= item.id %>"><p style="display: inline-block;"><%= item.text %></p></a>

              <a href ="/doneItem/<%= item.id %>">✔</a>
            </li>
          <% } %>
        <% } %>
      </ul>
    </div>

    <form action="/" method="post">
      <label>Que dois-je faire ?</label>
      <input type="text" name="item">
      <button type="submit">Valider</button>
    </form>

    <br>

    <a href="/allItemsDone">Voir ma ToDoList faite</a>
  </body>
</html>
